let OAUTH_ENDPOINT = "https://id.twitch.tv/oauth2/token";

function handleAuth() {
	let username = config.twitchChannelPoints.username;
	let clientID = config.twitchChannelPoints.clientID;
	let clientSecret = config.twitchChannelPoints.clientSecret;
}